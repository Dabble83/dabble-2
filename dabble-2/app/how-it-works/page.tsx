export const metadata = {
  title: "How it works — Dabble 2.0",
};

export default function HowItWorksPage() {
  return (
    <div className="ui-container py-16">
      <p className="ui-label">Credits & sessions</p>
      <h1 className="mt-3 font-serif font-semibold tracking-tight text-[var(--text-primary)] [font-size:var(--fs-h1)]">
        How it works
      </h1>
      <p className="mt-4 max-w-2xl font-sans leading-relaxed text-[var(--text-secondary)] [font-size:var(--fs-body)]">
        This page will explain credits, teaching vs learning, and session norms. Content will align
        with the credits RFC and Master Plan §5.7; for now it anchors the home-page explainer link.
      </p>
    </div>
  );
}
